<div class="container" >
<h1>All Products</h1>

<% @products.each do |product| %>
  <div>
  <ul class="list-group">
      <a href="/products/<%= product.id %>">
      <h4> <%= product.id %> | <%= product.title %></h4>
      </a>
      <hr>
  </ul>
  </div>

  
<% end %>
</div>